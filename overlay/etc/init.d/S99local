#!/bin/sh

# for debug
mount -t debugfs debugfs /sys/kernel/debug

if [ -e /dev/mmcblk1p3 ]; then
	# mount sdcard partition 3 to /opt
	mount /dev/mmcblk1p3 /opt
else
	# mount emmp partition 3 to /opt
	mount /dev/mmcblk0p3 /opt
fi

if [ -e /opt/postup.sh ]; then
	. /opt/postup.sh
fi

